export default async function decorate(block) {
  // Footer nav columns: single row with 4 cells (one per column)
  // Each cell contains an h6 heading and a ul list of links
  // No additional restructuring needed — CSS grid handles layout
}
