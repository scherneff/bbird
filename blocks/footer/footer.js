export default async function decorate(block) {
  // Footer is loaded as a fragment — inner blocks (footer-nav)
  // are decorated by the EDS framework via loadFragment().
  // No additional DOM manipulation needed here.
}
