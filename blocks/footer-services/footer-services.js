export default async function decorate(block) {
  // Footer services grid: each row has 7 cells (service items)
  // Each cell contains a single <a> with <strong> title and text description
  // No additional restructuring needed — CSS grid handles layout
}
